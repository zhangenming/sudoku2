export type Item = {
  v: string
  maybe: Set<string>
  maybeDel: Set<string>
  resolveL: string
  resolveRow: String
  resolveCol: String
  resolveBlc: String
  blk: number
  item: number
  i: number
  j: number
}

export type Items = Item[]
